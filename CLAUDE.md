# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

This repository is empty — no commits exist yet (locally or on `origin`). There is no build system, test framework, or source code to document.

- Remote: https://github.com/dkdlqoddi/dev-env-blindspot.git
- Default branch: `main`

## Instructions

Once the project takes shape (language, build tooling, tests), regenerate this file with `/init` so it reflects the actual commands and architecture.
