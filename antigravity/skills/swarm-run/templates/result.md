# T01 결과

- 상태: 완료 | 부분 완료 | 실패
- 시도: 1
- 바꾼 파일: `path/a.py`, `path/b.py` (신규)
- 검증: 통과 | 실패 | 실행 안 함 — (key message in 1–3 lines when it failed)
- 브리프와 다르게 한 것: 없음 | (what and why)
- 결정: 없음 | (one line per non-obvious choice: what you chose and why)
- 막힌 것: 없음 | (a change needed outside 소유 파일, or an impossible or contradictory instruction: the file and the change)

<!-- Written by the swarm-worker subagent at the end of its task: T01 becomes its id, and 시도 the attempt number in its prompt (swarm_next.py ignores a result whose 시도 differs). Keep every label, and pick one 상태 and one 검증 value spelled exactly as here; write the free text in English. Every bullet stays; 없음 is a valid value. Under 40 lines, no logs. swarm_next.py reads the 상태, 시도, and 막힌 것 lines; swarm-auditor (Claude Code) reads the rest. -->
